version: "3"

services:
  reverse-proxy:
    image: nginx:alpine
    volumes:
      - $PWD/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80
    restart: always

  web:
    image: httpd:alpine
    volumes:
      - $PWD/html:/usr/local/apache2/htdocs
    restart: always

