services:
  reverse-proxy:
    image: nginx:alpine
    volumes:
      - $PWD/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80
    restart: always

  web:
    image: nginx:alpine
    volumes:
      - $PWD/html:/usr/share/nginx/html
    restart: always

